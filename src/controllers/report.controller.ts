import { Request, Response } from 'express';

// Stubs for Report Controller
export const generatePdfInvoice = async (req: Request, res: Response) => { res.json({ message: 'Generate PDF Invoice Stub' }); };
export const exportExcelReport = async (req: Request, res: Response) => { res.json({ message: 'Export Excel Report Stub' }); };
